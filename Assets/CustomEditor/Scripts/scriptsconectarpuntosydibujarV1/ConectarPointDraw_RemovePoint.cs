using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ConectarPointDraw_RemovePoint : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    public void RemovePoint()
    {
//        ConectarConectarPointDraw_LrTestigScriptLineaFija.points.RemoveAt(ConectarConectarPointDraw_LrTestigScriptLineaFija.points.Count - 1);
    }
}
